create table Stu(
name varchar(20),
school varchar(50),
obiect varchar(20),
sex varchar(10),

	);
create table exam(
id int,
name varchar,
math decimal,
english decimal,
);

select * from exam order by chinese desc limit 1,1